version: "3"

networks:
  nginx_nginx-network:
    external: true

services:
  server:
    image: ghcr.io/bnorm/arcade-server:latest
    restart: unless-stopped
    networks:
      - nginx_nginx-network
    environment:
      PORT: 8080
