# Rally

## Demo

From the root project directory, run the following Gradle tasks:

```
gradle testRally
```

Use the UI to load up to 6 racers from `games/rally/rally-samples/build/racers`.
Then start a race!
